// Re-export from context so existing import paths keep working.
export type { ConsentStatus } from '@/context/CookieConsentContext';
export { useCookieConsent } from '@/context/CookieConsentContext';
